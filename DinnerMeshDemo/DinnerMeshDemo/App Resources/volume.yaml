volume:
  schemaVersion: 1.0.0-preview1
  name: dinnerVolume
  properties:
    description: Azure Files storage volume for dinner App.
    provider: SFAzureFile
    azureFileParameters: 
        shareName: "[parameters('shareName')]"
        accountName: "[parameters('accountName')]"
        accountKey: "[parameters('accountKey')]"
